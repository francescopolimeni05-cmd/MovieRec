# Runtime dependencies — also all the deployed Streamlit app needs.
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7        # EDA / comparison figures (generate_eda.py, tuning.py)
streamlit>=1.30        # interactive demo UI: `streamlit run app.py`
# Local extra (only to run notebooks/recommender_walkthrough.ipynb):
#   pip install jupyter
# Optional MF / implicit-feedback libraries (not required):
#   scikit-surprise>=1.1.4
#   implicit>=0.7
